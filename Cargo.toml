[workspace]
members = [
   'day01',
   'day02',
]

# Prevent warning:
# virtual workspace defaulting to `resolver = "1"` despite one or more workspace members being on edition 2021
# which implies `resolver = "2"`
resolver = "2"
